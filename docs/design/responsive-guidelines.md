# レスポンシブガイドライン: Fortune Telling

> Generated by CCAGI SDK v3.13.0 | Phase 2: Responsive Guidelines
> Date: 2026-03-08

---

## 1. モバイルファースト設計方針

### 1.1 基本原則

| 原則 | 説明 |
|------|------|
| **モバイルファースト** | CSSはモバイル（320px〜）を基準に記述し、`min-width`で拡張する |
| **片手操作** | スマホでの片手操作を考慮した配置設計 |
| **コンテンツ優先** | 小さい画面ではコンテンツを最優先、装飾は段階的に追加 |
| **プログレッシブ・エンハンスメント** | 大画面になるにつれて情報密度と装飾を段階的に増やす |

### 1.2 ブレークポイント

| 名称 | サイズ | デバイス | レイアウト |
|------|--------|---------|-----------|
| base | 0〜639px | スマートフォン（縦） | 1カラム |
| sm | 640px〜 | スマートフォン（横） | 1カラム（余白拡大） |
| md | 768px〜 | タブレット | 2カラム可 |
| lg | 1024px〜 | ノートPC | サイドバー + メイン |
| xl | 1280px〜 | デスクトップ | サイドバー + メイン + サブ |

---

## 2. ナビゲーション設計

### 2.1 モバイル: 下部タブバー

```
構成:
  高さ: 64px（Safe Area含む）
  アイテム数: 最大5
  アクティブ表示: Soft Gold (#c9a96e) アイコン + ラベル
  非アクティブ: Silver Mist (#b8b0c8)
  背景: カード背景色 + 上部ボーダー

タブ項目:
  1. ホーム（ダッシュボード）
  2. チャット（AI相談）
  3. 命盤（基本性質）
  4. カレンダー
  5. 設定
```

#### タッチターゲット要件

| 項目 | 最小値 | 推奨値 |
|------|--------|--------|
| タップ領域 | 44px x 44px | 48px x 48px |
| タブバーアイテム | 幅: 画面幅/5 | - |
| アイコンサイズ | 24px | 24px |
| ラベルサイズ | 10px | 12px |

### 2.2 デスクトップ（lg以上）: サイドバー

```
構成:
  幅: 240px（固定）
  折りたたみ時: 64px（アイコンのみ）
  位置: 左固定
  背景: card背景色
  右ボーダー: border色

ナビ項目:
  - アイコン + ラベル（展開時）
  - アイコンのみ + Tooltip（折りたたみ時）
  - アクティブ: Soft Gold アクセント
  - ホバー: 背景色の微妙な変化
```

### 2.3 切替ロジック

```
base〜md: 下部タブバー表示、サイドバー非表示
lg〜:     サイドバー表示、下部タブバー非表示
```

---

## 3. 画面別レスポンシブ設計

### 3.1 ダッシュボード（ホーム）

#### モバイル（base）

```
レイアウト: 1カラム、縦スクロール
  ├── 今日の日付 + 干支（上部固定風）
  ├── 今日のアドバイスカード（フル幅）
  ├── 仕事運カード（フル幅）
  ├── プライベート運カード（フル幅）
  ├── 吉方位ミニ方位盤
  └── クイックアクション（横スクロール）
```

#### タブレット（md）

```
レイアウト: 2カラムグリッド
  ├── 今日の日付 + 干支（上部、フル幅）
  ├── 今日のアドバイスカード（フル幅）
  ├── 仕事運カード | プライベート運カード（2カラム）
  ├── 吉方位 | クイックアクション（2カラム）
  └── 背景に星座パターン表示開始
```

#### デスクトップ（lg〜）

```
レイアウト: サイドバー + メインエリア
  サイドバー（240px）
  メイン:
    ├── 上部: 日付 + 干支 + グリーティング
    ├── メイン: アドバイスカード（大）
    ├── 2カラム: 仕事運 | プライベート運
    ├── 方位盤（拡大表示）
    └── 背景装飾フル表示
```

### 3.2 AI相談チャット

#### モバイル（base）

```
レイアウト: フルスクリーン
  ├── ヘッダー: 戻るボタン + タイトル（48px）
  ├── メッセージエリア: flex-1、スクロール可
  │   ├── AIバブル（左寄せ、最大幅80%）
  │   └── ユーザーバブル（右寄せ、最大幅80%）
  ├── サジェスト質問: 横スクロール（入力欄上部）
  └── 入力エリア: 下部固定（Safe Area対応）
      ├── テキスト入力
      └── 送信ボタン（44px x 44px）
```

#### タブレット（md）

```
レイアウト: フルスクリーン（余白拡大）
  ├── メッセージ最大幅: 640px（中央寄せ）
  ├── バブル最大幅: 70%
  └── サジェスト質問: 折り返しグリッド
```

#### デスクトップ（lg〜）

```
レイアウト: サイドバー + チャットエリア
  サイドバー（240px）
  チャットエリア:
    ├── メッセージ最大幅: 720px（中央寄せ）
    ├── バブル最大幅: 60%
    ├── AIバブルに参照ナレッジ展開可能
    └── サジェスト質問: グリッド2〜3カラム
```

#### チャットUI共通ルール

| 項目 | ルール |
|------|--------|
| 入力欄の高さ | 最小48px、最大120px（自動伸縮） |
| キーボード表示時 | 入力欄がキーボード上に追従 |
| メッセージ送信 | Enter送信（デスクトップ）、送信ボタン（モバイル） |
| スクロール | 新メッセージで自動スクロール |
| 入力中表示 | AIの入力中インジケーター（3ドットアニメーション） |

### 3.3 基本性質ページ

#### モバイル（base）

```
レイアウト: 1カラム、縦スクロール
  ├── 命盤チャート（横スクロール可、ピンチズーム対応）
  ├── 性質カテゴリ: アコーディオン形式
  │   ├── 事業適性
  │   ├── 対人関係
  │   ├── 健康
  │   └── ...
  └── プロ鑑定士コメント引用ブロック
```

#### デスクトップ（lg〜）

```
レイアウト: 命盤チャート（メイン） + 詳細パネル（サブ）
  ├── 命盤チャート: インタラクティブ（ホバーで詳細）
  ├── 右パネル: 選択した宮の詳細
  └── 下部: プロ鑑定士コメント
```

### 3.4 カレンダー

#### モバイル（base）

```
レイアウト: フル幅カレンダー
  ├── 月切替ヘッダー
  ├── 月間グリッド（7カラム、フル幅）
  │   ├── 吉日: 金ドット
  │   └── 凶日: 深紅ドット
  ├── 日付タップ → Sheet（下からスライド）
  └── 月の全体運（上部に小さく表示）
```

#### デスクトップ（lg〜）

```
レイアウト: カレンダー + 詳細パネル
  ├── 左: 月間カレンダー（拡大）
  └── 右: 選択日の詳細パネル（常時表示）
```

### 3.5 方位マップ

#### モバイル（base）

```
レイアウト: 中央配置コンパス
  ├── 八方位コンパス（画面幅の80%）
  ├── 吉方位タップで詳細表示
  └── 下部に方位別運勢リスト
```

#### デスクトップ（lg〜）

```
レイアウト: 大型コンパス + サイドパネル
  ├── 中央: 大型コンパスUI
  └── 右: 方位詳細パネル
```

---

## 4. タッチターゲットガイドライン

### 4.1 最小サイズ

| 要素 | 最小サイズ | 推奨サイズ |
|------|----------|-----------|
| ボタン（プライマリ） | 44px x 44px | 48px x 48px |
| ボタン（セカンダリ） | 44px x 36px | 44px x 44px |
| タブバーアイテム | 44px x 44px | - |
| リストアイテム | 高さ44px | 高さ48px |
| アイコンボタン | 44px x 44px | 48px x 48px |
| チェックボックス | 44px x 44px | - |
| カレンダー日付セル | 44px x 44px | - |
| チャット送信ボタン | 44px x 44px | 48px x 48px |

### 4.2 間隔

| 要素間 | 最小間隔 |
|--------|---------|
| タップ可能要素同士 | 8px |
| ボタン同士 | 12px |
| リストアイテム間 | 1px（ボーダーで区切り） |

---

## 5. PWA表示設定

### 5.1 manifest.json 設定

```json
{
  "name": "Fortune Telling - 紫微斗数パーソナル鑑定",
  "short_name": "Fortune",
  "display": "standalone",
  "orientation": "portrait-primary",
  "theme_color": "#1a1432",
  "background_color": "#0d0b14",
  "start_url": "/",
  "scope": "/"
}
```

### 5.2 表示モード対応

| 項目 | 設定 |
|------|------|
| display | standalone |
| orientation | portrait（スマホ）、any（タブレット以上） |
| status-bar-style | black-translucent |
| theme-color | #1a1432（Deep Indigo） |
| background-color | #0d0b14（Midnight） |

### 5.3 Safe Area対応

```css
/* iOS Safe Area */
padding-top: env(safe-area-inset-top);
padding-bottom: env(safe-area-inset-bottom);
padding-left: env(safe-area-inset-left);
padding-right: env(safe-area-inset-right);
```

| 要素 | Safe Area対応 |
|------|--------------|
| ヘッダー | top inset |
| 下部タブバー | bottom inset |
| チャット入力欄 | bottom inset |
| サイドバー | left inset（横向き時） |

### 5.4 ホームアイコン

| 項目 | 仕様 |
|------|------|
| デザイン | 深インディゴ背景 + 金の星図モチーフ |
| サイズ | 192x192, 512x512 |
| 形状 | maskable対応（安全領域内にデザイン） |
| 角丸 | OSに委ねる |

### 5.5 スプラッシュスクリーン

| 項目 | 仕様 |
|------|------|
| 背景色 | #0d0b14（Midnight） |
| ロゴ | 金の星図モチーフ（中央配置） |
| アニメーション | フェードイン（200ms） |

---

## 6. フォントサイズのレスポンシブ

### 6.1 見出しスケール

| 要素 | base | md | lg |
|------|------|-----|-----|
| h1 | 1.75rem (28px) | 2rem (32px) | 2.25rem (36px) |
| h2 | 1.5rem (24px) | 1.75rem (28px) | 1.875rem (30px) |
| h3 | 1.25rem (20px) | 1.375rem (22px) | 1.5rem (24px) |
| h4 | 1.125rem (18px) | 1.125rem (18px) | 1.25rem (20px) |

### 6.2 本文

| 要素 | base | md | lg |
|------|------|-----|-----|
| body | 0.9375rem (15px) | 1rem (16px) | 1rem (16px) |
| small | 0.8125rem (13px) | 0.875rem (14px) | 0.875rem (14px) |
| caption | 0.6875rem (11px) | 0.75rem (12px) | 0.75rem (12px) |

---

## 7. レスポンシブ実装チェックリスト

### 全画面共通

- [ ] モバイル（320px）で崩れないことを確認
- [ ] タッチターゲットが44px以上
- [ ] Safe Area対応（iOS）
- [ ] 横向き表示に対応
- [ ] prefers-reduced-motion 対応
- [ ] ダークモード / ライトモード両方で確認
- [ ] フォントサイズが読みやすいサイズ
- [ ] コンテンツが画面外にはみ出さない
- [ ] 横スクロールが意図しない箇所で発生しない

### ナビゲーション

- [ ] モバイル: 下部タブバーが正しく表示
- [ ] デスクトップ: サイドバーが正しく表示
- [ ] 切替がlgブレークポイントで正しく動作
- [ ] サイドバーの折りたたみが正しく動作

### チャットUI

- [ ] キーボード表示時に入力欄が隠れない
- [ ] 長文メッセージの折り返し
- [ ] 画像・参照ナレッジの表示
- [ ] サジェスト質問の表示とスクロール
- [ ] メッセージエリアのスクロール動作

---

*Generated by CCAGI SDK v3.13.0 -- Phase 2: Responsive Guidelines*
